import React from "react";
import preloader from "../../images/Ghost.gif";

export const Preloader = () => {
    return (
        <div>
            <img src={preloader} alt=""/>
        </div>
    )
}